import 'package:flutter/material.dart';

class Module6Theory extends StatelessWidget {
  const Module6Theory({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      padding: const EdgeInsets.all(16.0),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(
            "В Android работа с местоположением пользователя строится на основе высокоуровневой абстракции над физическими сенсорами: GPS-приёмниками, Wi-Fi-модулями, базовыми станциями сотовой связи и даже инерциальными датчиками."
                "\nПонимание архитектуры геолокационных API критически важно не только для корректной реализации функциональности, но и для соблюдения требований конфиденциальности и энергоэффективности. Android никогда не отдаёт приложению «голые» данные с GPS-чипа. Вместо этого он предоставляет провайдеры местоположения — логические источники данных, каждый из которых обладает своими характеристиками точности, энергопотребления и доступности.",
            style: Theme.of(context).textTheme.bodyMedium,
          ),
          const SizedBox(height: 8),
          Text(
            'Провайдеры местоположения: точный и приближённый',
            style: Theme.of(context).textTheme.titleMedium,
          ),
          const SizedBox(height: 8),
          Text(
            "В Android существует два основных провайдера местоположения: GPS_PROVIDER и NETWORK_PROVIDER. Их различие лежит не только в технической реализации, но и в философии использования."
                "\nGPS_PROVIDER использует спутниковую систему позиционирования. Он обеспечивает высокую точность — до нескольких метров на открытом пространстве — но требует прямой видимости неба, долго «холодно стартует» и интенсивно расходует заряд батареи. Этот провайдер подходит для задач, где важна геометрическая точность: навигация, геотеггинг фотографий, спортивные трекеры."
                "\nNETWORK_PROVIDER, напротив, определяет местоположение на основе данных о ближайших вышках сотовой связи и доступных Wi-Fi сетях. Он работает даже в помещениях, быстро даёт первую оценку координат и потребляет значительно меньше энергии. Однако его точность ограничена — от десятков до сотен метров. Такой подход оправдан в приложениях, где важна «городская привязка», а не метровая точность: погодные сервисы, рекомендации ближайших кафе, геотаргетированная реклама."
                "\nИнтересно, что Android не заставляет разработчика выбирать один из провайдеров. Напротив, система поощряет гибридные стратегии: например, использовать NETWORK_PROVIDER для быстрого получения грубой оценки, а затем уточнять её с помощью GPS_PROVIDER, если пользователь остаётся на месте достаточно долго. Более того, начиная с Android 4.4, появился FusedLocationProvider, который сам принимает такие решения, абстрагируя разработчика от низкоуровневых деталей. Однако понимание классических провайдеров остаётся необходимым для диагностики проблем и понимания поведения системы на старых устройствах.",
            style: Theme.of(context).textTheme.bodyMedium,
          ),
          const SizedBox(height: 8),
          Image.asset(
            "assets/images/gpsManager.jpg",
            fit: BoxFit.contain,
            errorBuilder: (context, error, stackTrace) {
              return Container(
                height: 150,
                color: Colors.grey.shade300,
                child: const Icon(Icons.image_not_supported),
              );
            },
          ),
          const SizedBox(height: 8),
          Text(
            'Архитектура LocationManager',
            style: Theme.of(context).textTheme.titleMedium,
          ),
          const SizedBox(height: 8),
          Text(
            "Доступ к геолокационным данным в классической модели Android осуществляется через системный сервис LocationManager. Это не объект, который создаётся вручную, а компонент, управляемый операционной системой, к которому приложение получает доступ через механизм getSystemService(). Такой подход гарантирует, что все приложения используют один и тот же источник данных, избегая конфликтов и дублирования запросов к аппаратным сенсорам."
                "\nЦентральный метод взаимодействия с LocationManager — requestLocationUpdates(). Он подписывает приложение на получение обновлений координат от указанного провайдера. Важно понимать, что этот вызов не «запрашивает одно обновление», а регистрирует постоянный интерес к изменению местоположения. Система будет продолжать посылать обновления до тех пор, пока приложение явно не отпишется с помощью removeUpdates() или не будет завершено."
                "\nМеханизм доставки данных реализован через паттерн наблюдатель (Observer). Разработчик создаёт экземпляр LocationListener — интерфейса с четырьмя методами, каждый из которых реагирует на определённое событие. Ключевой из них — onLocationChanged(Location location). Этот метод вызывается каждый раз, когда система определяет новое местоположение, и передаёт в него объект класса Location, содержащий не только координаты, но и дополнительные метаданные: время определения, точность (в метрах), скорость, направление движения и даже этаж в здании (если данные доступны)."
                "\nНиже — минимальный пример регистрации на обновления от GPS:",
            style: Theme.of(context).textTheme.bodyMedium,
          ),
          const SizedBox(height: 8),
          Container(
            padding: const EdgeInsets.all(8.0),
            decoration: BoxDecoration(
              color: Colors.grey.shade800,
              borderRadius: BorderRadius.circular(4.0),
            ),
            child: Text(
              """
LocationManager locationManager = (LocationManager) getSystemService(Context.LOCATION_SERVICE);
LocationListener locationListener = new LocationListener() {
    @Override
    public void onLocationChanged(Location location) {
        double latitude = location.getLatitude();
        double longitude = location.getLongitude();
        // Обработка новых координат
    }
    // Остальные методы интерфейса (onStatusChanged, onProviderEnabled, onProviderDisabled)
    // можно оставить пустыми, если они не нужны
};
locationManager.requestLocationUpdates(LocationManager.GPS_PROVIDER, 0, 0, locationListener);
              """,
              style: const TextStyle(
                color: Colors.white,
                fontFamily: 'monospace',
              ),
            ),
          ),
          const SizedBox(height: 8),
          Text(
            "Обратите внимание на два последних параметра requestLocationUpdates(): минимальный временной интервал (в миллисекундах) и минимальное расстояние (в метрах), которые должны пройти между обновлениями. Установка обоих в ноль означает «присылай обновления как можно чаще», что крайне неэффективно с точки зрения энергопотребления. В реальных приложениях эти значения всегда подбираются с учётом задачи: для навигации — каждые 1–2 секунды и 5 метров, для погоды — раз в 15 минут и при смене города.",
            style: Theme.of(context).textTheme.bodyMedium,
          ),
          const SizedBox(height: 8),
          Text(
            'Безопасность и приватность',
            style: Theme.of(context).textTheme.titleMedium,
          ),
          const SizedBox(height: 8),
          Text(
            "Геолокационные данные — это персональная информация высшей категории чувствительности. Местоположение пользователя может раскрыть его привычки, социальный статус, религиозные предпочтения и даже состояние здоровья. Поэтому Android с самого начала строил свою модель безопасности на принципе явного согласия."
                "\nДля работы с геолокацией приложение обязано объявить в файле AndroidManifest.xml одно из двух разрешений:"
                "\nACCESS_FINE_LOCATION — для доступа к провайдерам высокой точности (GPS)."
                "\nACCESS_COARSE_LOCATION — для доступа только к приближённым данным (сеть)."
                "\nЭти разрешения относятся к категории опасных (dangerous), начиная с Android 6.0 (API 23). Это означает, что простого объявления в манифесте недостаточно. Приложение должно динамически запросить разрешение у пользователя во время выполнения. Если пользователь отказывает, все вызовы к LocationManager будут завершаться с ошибкой или просто игнорироваться. Это фундаментальный сдвиг в модели безопасности: разработчик больше не может рассчитывать на то, что разрешение «уже есть»."
                "\nКроме того, система предоставляет пользователю полный контроль: в настройках он может в любой момент отозвать разрешение для конкретного приложения или изменить его режим с «только во время использования» на «никогда». Современные приложения обязаны грациозно обрабатывать такие сценарии, не падая и не создавая негативного пользовательского опыта.",
            style: Theme.of(context).textTheme.bodyMedium,
          ),
          const SizedBox(height: 8),
          Image.asset(
            "assets/images/navigation.png",
            fit: BoxFit.contain,
            errorBuilder: (context, error, stackTrace) {
              return Container(
                height: 150,
                color: Colors.grey.shade300,
                child: const Icon(Icons.image_not_supported),
              );
            },
          ),
          const SizedBox(height: 8),
          Text(
            'Современный подход',
            style: Theme.of(context).textTheme.titleMedium,
          ),
          const SizedBox(height: 8),
          Text(
            "Хотя LocationManager и LocationListener остаются рабочим инструментом, Google уже много лет продвигает более совершенную и энергоэффективную альтернативу — Fused Location Provider (FLP) из состава Google Play Services. FLP не просто объединяет данные от GPS и сети, но и использует сенсорное слияние (sensor fusion): он анализирует показания акселерометра, гироскопа и магнитометра, чтобы понять, движется ли пользователь пешком, на велосипеде или в автомобиле, и адаптирует частоту и источник запросов к местоположению соответствующим образом."
                "\nFLP предлагает два ключевых метода: getLastLocation() для моментального получения последней известной позиции и requestLocationUpdates() для подписки на изменения. Он работает с гораздо меньшим потреблением энергии и предоставляет более стабильные и точные данные благодаря алгоритмам фильтрации и предсказания на стороне Google."
                "\nТем не менее, знание классического LocationManager остаётся важным. Во-первых, FLP требует наличия Google Play Services, что не всегда выполняется (например, на китайских устройствах). Во-вторых, понимание базовых механизмов позволяет лучше отлаживать и проектировать архитектуру, даже если вы используете высокоуровневые абстракции. В учебных целях работа с LocationManager — это превосходный способ погрузиться в суть проблемы, прежде чем перейти к её упрощённому решению.",
            style: Theme.of(context).textTheme.bodyMedium,
          ),
          const SizedBox(height: 8),
        ],
      ),
    );
  }
}